---
phase: 03-projects-collections
plan: 02
type: execute
wave: 1
depends_on: ["03-01"]
files_modified: [src/components/Header/Header.jsx, src/pages/GalleryPage.jsx, src/pages/GalleryPage.css]
autonomous: false
---

<objective>
## Goal
Add "Projects" to the site header navigation and enhance the collection drill-down view with date and tags metadata.

## Purpose
Makes Projects discoverable from any page (not just via Gallery category filter) and gives the collection view a richer "project page" feel with date/tags context.

## Output
- Header navigation updated with "Projects" link (navigates to gallery with projects filter)
- Collection drill-down enhanced with date and tags display
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md

## Prior Work
@.paul/phases/03-projects-collections/03-01-SUMMARY.md
- Collection data model and helpers (collectionHelpers.js)
- "Projects" category in Gallery with collection card grid and drill-down
- Drill-down currently shows: collection name, description, photo count, masonry grid + back button
- Drill-down does NOT show: collection date, tags

## Source Files
@src/components/Header/Header.jsx
@src/components/Header/Header.css
@src/pages/GalleryPage.jsx
@src/pages/GalleryPage.css
@src/styles/global.css
</context>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /frontend-design | required | Before writing JSX components | ○ |
| /ui-ux-pro-max | required | When writing CSS for metadata display | ○ |
| /bencium-controlled-ux-designer | required | For visual decisions on metadata layout | ○ |

**BLOCKING:** Required skills MUST be loaded before APPLY proceeds.

## Skill Invocation Checklist
- [ ] /frontend-design loaded (run command or confirm)
- [ ] /ui-ux-pro-max loaded (run command or confirm)
- [ ] /bencium-controlled-ux-designer loaded (run command or confirm)

</skills>

<acceptance_criteria>

## AC-1: Header Navigation Includes Projects
```gherkin
Given the site header is visible on any page
When a user views the navigation links
Then "Projects" appears as a nav item (numbered consistently with other links)
And clicking "Projects" navigates to the gallery with the projects category active
And "Projects" shows an active indicator when viewing the projects category or any collection drill-down (category=projects with or without collection param) <!-- audit-added -->
And "Gallery" shows an active indicator when viewing other gallery categories (all, portraits, film, landscape)
```

## AC-2: Collection Drill-Down Metadata
```gherkin
Given a user is viewing a collection's photos (drill-down view)
When the collection header is displayed
Then the collection date is shown (formatted as month and year, e.g., "Feb 2024")
And the collection tags are displayed as styled pills (matching lightbox tag pattern)
And metadata appears below the description and above the photo count
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add Projects link to Header navigation</name>
  <files>src/components/Header/Header.jsx</files>
  <action>
    1. Update the `links` array to include Projects between Gallery and About:
       ```js
       const links = [
         { to: '/', label: 'Work', num: '01' },
         { to: '/gallery', label: 'Gallery', num: '02' },
         { to: '/gallery?category=projects', label: 'Projects', num: '03' },
         { to: '/about', label: 'About', num: '04' },
       ];
       ```

    2. Update the active state check to handle query params:
       - Current: `location.pathname === to`
       - New: Compare the full `pathname + search` for links with query params
       - For `/gallery` (no params): active when pathname is /gallery AND search does NOT contain category=projects
       - For `/gallery?category=projects`: active when pathname is /gallery AND search contains category=projects (regardless of other params like &collection=xxx) <!-- audit-added -->
       - For simple links (/, /about): keep existing pathname check
       - Implementation approach: create a helper function `isLinkActive(link, location)` that handles both cases

    Avoid: Breaking the mobile menu — the same links array feeds both desktop nav and mobile overlay.
    Avoid: Changing the visual style of nav items — only add the new entry and update active logic.
  </action>
  <verify>Navigate between pages — confirm Projects link appears, active states work correctly for all nav items. Confirm mobile menu also shows Projects.</verify>
  <done>AC-1 satisfied: Projects link in header, correct active states for all navigation items</done>
</task>

<task type="auto">
  <name>Task 2: Add date and tags to collection drill-down header</name>
  <files>src/pages/GalleryPage.jsx, src/pages/GalleryPage.css</files>
  <action>
    1. In GalleryPage.jsx, when `isCollectionView` is true, add metadata below the description:
       - Format collection date as readable month/year (e.g., "2024-02" → "Feb 2024")
       - If date is missing or unparseable, omit the date element entirely (defensive) <!-- audit-added -->
       - Display collection tags as styled pills
       - Use a simple date formatter (new Date parsing or manual month map — no new dependencies)

    2. In GalleryPage.css, add styles for collection metadata:
       - Date: mono font, small caps, ember color (matching lightbox date pattern)
       - Tags: pill/chip style with ember border (matching lightbox tag pattern from Lightbox.css)
       - Container: flex row with gap, centered below description, above photo count
       - Responsive: wrap on mobile

    Avoid: Adding new npm dependencies for date formatting.
    Avoid: Changing the collection card grid view (projects overview) — only enhance the drill-down header.
  </action>
  <verify>Click into a collection from Projects category. Confirm date (e.g., "Feb 2024") and tags (e.g., "film", "gold") display below description. Confirm styling matches existing design system.</verify>
  <done>AC-2 satisfied: Collection date and tags visible in drill-down header</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Header navigation with Projects link + collection drill-down metadata</what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Visit: http://localhost:5173/
    3. Confirm: Header shows "01 Work", "02 Gallery", "03 Projects", "04 About"
    4. Click: "Projects" in header
    5. Confirm: Gallery shows projects category view, "Projects" nav item is active
    6. Click: "Gallery" in header
    7. Confirm: Gallery shows "All Work", "Gallery" nav item is active
    8. Click: "Projects" category filter, then click a collection card
    9. Confirm: Drill-down header shows date (e.g., "Feb 2024") and tags as pills
    10. Confirm: Mobile menu shows all 4 nav items correctly
    11. Confirm: Tab key navigates through all 4 header links with correct focus order <!-- audit-added -->
    12. Confirm: "Projects" remains active when inside a collection drill-down <!-- audit-added -->
    13. Confirm: No visual regression on existing pages
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- src/components/Gallery/* (gallery component stable)
- src/components/Lightbox/* (lightbox stable)
- src/components/ContactForm/* (contact form stable)
- src/components/CategoryFilter/* (category filter stable)
- src/styles/global.css (design tokens locked)
- src/data/photos.json (data model locked from 03-01)
- src/utils/collectionHelpers.js (helpers stable from 03-01)

## SCOPE LIMITS
- No new routes or pages
- No new npm dependencies
- No changes to collection card grid (projects overview)
- No changes to Gallery masonry layout or Lightbox

</boundaries>

<verification>
Before declaring plan complete:
- [ ] `npm run build` passes without errors
- [ ] Header shows Projects link on all pages
- [ ] Active state correct: Projects active on projects view, Gallery active on other gallery views
- [ ] Mobile menu shows all 4 nav items
- [ ] Collection drill-down shows date and tags
- [ ] Tags styled as pills matching existing design patterns
- [ ] No console errors or warnings
- [ ] Existing pages function without regression
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Header navigation feels natural with 4 items
- Collection metadata enhances the drill-down experience
</success_criteria>

<output>
After completion, create `.paul/phases/03-projects-collections/03-02-SUMMARY.md`
</output>
